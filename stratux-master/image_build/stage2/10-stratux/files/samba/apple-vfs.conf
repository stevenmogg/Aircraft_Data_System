# Apple / iOS SMB client compatibility (included from [global] in smb.conf)
   server min protocol = SMB2
   vfs objects = fruit streams_xattr
   fruit:metadata = stream
   fruit:model = RackMac
   fruit:posix_rename = yes
   fruit:veto_appledouble = no
   fruit:nfs_aces = no
